1. complete master

2. general integration test.

4. worker down?

5. master down?

6. beautiful progress in #TODO: `local_monitoring`

7. multiprocessing map task:
from multiprocessing import Pool

def f(x):
    return x*x

if __name__ == '__main__':
    with Pool(5) as p:
        print(p.map(f, [1, 2, 3]))